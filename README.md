# Sekin Vladislav

## Небольшое пояснение к таску

- Я немного изменил функциональность `EnrichmentService`. У меня он называется `Enricher` и редактирует сообщение по ссылке. Таким образом, можно, используя `ConcurrentHashMap` параллельно запустить все процессы enrichment'а.

- Обертка, которая позволяет это сделать – EnrichmentExecutor. Для него есть отдельные тесты, сквозные, которые требует тз. (`EnrichmentExecutorTest`)

- Не очень представляю, как надо было исплользовать `EnrichmentService`, возвращающий копию `Message` в многопоточке (Делать `merge(message1, message2, ...)`), поэтому сделал так. Надеюсь, Я правильно понял тз)

